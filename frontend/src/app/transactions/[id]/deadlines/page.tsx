'use client';import {useEffect,useState} from 'react';import {useParams} from 'next/navigation';import {api} from '@/lib/api-client';import type {Transaction} from '@/types/domain';import {AppShell} from '@/components/workflow/AppShell';import {PageHeader} from '@/components/workflow/PageHeader';import {DeadlineList} from '@/components/workflow/DeadlineList';import {LoadingState} from '@/components/workflow/LoadingState';export default function Page(){const{id}=useParams<{id:string}>();const[tx,setTx]=useState<Transaction>();useEffect(()=>{api.getTransaction(id).then(setTx)},[id]);return <AppShell><PageHeader eyebrow="Dates and Deadlines" title="CTME deadline rows"/>{tx?<DeadlineList deadlines={tx.deadlines}/>:<LoadingState/>}</AppShell>}
