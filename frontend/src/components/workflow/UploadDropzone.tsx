'use client';import { useRef } from 'react';import { Button } from './Button';
export function UploadDropzone({onFile,busy}:{onFile:(file:File)=>void;busy?:boolean}){const ref=useRef<HTMLInputElement>(null);return <div className="upload-drop"><input ref={ref} type="file" accept="application/pdf" onChange={e=>{const f=e.target.files?.[0]; if(f) onFile(f)}}/><p className="eyebrow">CTME Contract Intake</p><h2>Upload Contract to Buy and Sell Real Estate PDF</h2><p>Creates a source document, deterministic extraction run, review queue, deadlines, tasks, contacts, documents, and audit history.</p><Button disabled={busy} onClick={()=>ref.current?.click()}>{busy?'Uploading…':'Choose PDF'}</Button></div>}
