'use client';import type { ExtractedField } from '@/types/domain';import { StatusBadge } from './StatusBadge';import { Button } from './Button';
export function ExtractedFieldRow({field,onApprove}:{field:ExtractedField;onApprove?:(f:ExtractedField)=>void}){return <tr><th>{field.label}</th><td>{field.value || '—'}</td><td>{Math.round(field.confidence*100)}%</td><td>p.{field.sourcePage} · {field.sourceSection}</td><td><StatusBadge label={field.populationStatus} tone={field.populationStatus==='not_applicable'?'neutral':field.populationStatus==='completed'?'success':'gold'}/></td><td><Button onClick={()=>onApprove?.(field)}>Approve</Button></td></tr>}
