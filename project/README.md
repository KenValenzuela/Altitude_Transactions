# Project assets

The active product demo source of truth is `frontend/src` (Next.js TypeScript) and `backend/app` (FastAPI/Python). Legacy JSX prototype files were archived under `project/archive/legacy-jsx-prototypes/` so they are clearly design references only, not runnable application source.
